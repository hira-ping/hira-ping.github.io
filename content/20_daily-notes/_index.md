+++
title = "Daily Notes"
weight = 3
chapter = true
menuPre = "<i class='fa-solid fa-file-pen' ></i> "
date = 2026-08-11
lastmod = 2026-09-22
+++

## 2026-09-22

一様収束性を復習したときに、ちょっと引っかかったところをしっかりとまとめた。
#### **量化記号の順序**

述語論理において、存在量化子（$\exists$）によって選ばれる変数の依存関係は、記述される量化記号の順序によって定まる。ある変数の存在を示す $\exists$ より前に置かれた量化子（$\forall$ や $\exists$）がある場合、その変数の選び方は前に出現した変数に依存して決めることができる。反対に、$\exists$ の後ろに置かれた $\forall$ の変数に対しては依存できず、それらすべてに対して共通の値を固定して選ぶ必要がある。

ここで、論理学の構造において変数の依存関係を整理する際、前に出た $\exists$ によって選ばれる変数は、さらに前に出た $\forall$ に依存して決まる関数として扱われる。そのため、連続する $\exists$ がある場合、後から出る $\exists$ の変数は直前の $\exists$ の変数に依存し得るが、直前の変数を介して、根本的にはそれ以前に出現したすべての $\forall$ に依存しているという階層構造をなしている。つまり、$\exists$ によって選ばれる変数の依存先は、最終的に前にあるすべての $\forall$ の変数に帰着する。

この依存関係の規則を、関数列の各点収束と一様収束を例に整理する。

#### 各点収束における変数の依存関係

各点収束の定義は以下の論理式で表される。

$$\forall \varepsilon > 0, \forall x \in A, \exists N \in \mathbb{N}, \forall n \geq N; \quad \vert{}f_n(x) - f(x)\vert{} < \varepsilon$$

この論理式の構造では、$\exists N$ の前に $\forall \varepsilon$ と $\forall x$ が配置されている。量化記号の規則に従うと、選ばれる自然数 $N$ は $\varepsilon$ と $x$ の両方に依存して決めることができる。これを明示的に書くと $N(\varepsilon, x)$ となる。

これは、ある $\varepsilon$ が与えられたとき、定義域 $A$ 内の点 $x$ の値に応じて、条件を満たすための $N$ を点ごとに個別に選び直すことが許容されていることを示している。

#### 一様収束における変数の依存関係

一様収束の定義は以下の論理式で表される。

$$\forall \varepsilon > 0, \exists N \in \mathbb{N}, \forall x \in A, \forall n \geq N; \quad \vert{}f_n(x) - f(x)\vert{} < \varepsilon$$

各点収束とは異なり、$\exists N$ の前には $\forall \varepsilon$ しか存在しない。$\forall x$ は $\exists N$ の後ろに配置されている。この順序により、選ばれる自然数 $N$ は $\varepsilon$ には依存できるが、$x$ には依存できなくなる。明示的に書くと $N(\varepsilon)$ となる。

これは、ある $\varepsilon$ が与えられたとき、$x$ の値を見る前に $N$ を決定しなければならないことを意味する。結果として、選ばれた $N$ は定義域 $A$ 内のすべての $x$ に対して共通して機能する必要がある。

#### 条件付き全称量化についての注記

定義中の $\forall n \geq N$ という記述は、一見すると変数 $n$ が $N$ に依存して選ばれるように見える。しかし、これは $\forall x \in A, \phi(x)$ が $\forall x, (x \in A \rightarrow \phi(x))$ の略記であることと同様に、含意（$\rightarrow$）を用いた条件付き全称量化の略記である。

厳密には、以下のように展開される構造が隠れている。

$$\forall n, (n \geq N \rightarrow \vert{}f_n(x) - f(x)\vert{} < \varepsilon)$$

ここで $n$ はすべての自然数について評価される変数であり、「$n \geq N$ が真ならば後の不等式も真である」という命題を構成している。$n$ を選ぶ操作が $N$ に依存しているわけではない。量化記号の順序によって前の変数に依存して具体的な値を選ぶ操作は、あくまで存在量化子（$\exists$）の変数について適用される。

このように、量化記号の配置順序は変数が何に依存して決定されるかを規定し、それが各点ごとに定まる性質（各点収束）か、定義域全体で共通して定まる性質（一様収束）かという違いを厳密に記述している。

#### 連続する同一量化子の順序交換

述語論理において、連続する全称量化子（$\forall$）の順序は入れ替えても論理的な意味は変わらない。すなわち、$\forall x, \forall y, \phi$ と $\forall y, \forall x, \phi$ は同値である。

各点収束の定義の冒頭にある $\forall \varepsilon > 0, \forall x \in A$ についても、$\forall x \in A, \forall \varepsilon > 0$ と順序を入れ替えて記述しても数学的な意味は同一である。この場合、後ろに続く $\exists N$ は、$\forall$ の配置順序に関わらず $\varepsilon$ と $x$ の両方に依存して選ぶことができる。

連続する存在量化子（$\exists$）同士の順序も交換可能である。順序の入れ替えによって依存関係や論理的な意味が変わるのは、これまでに確認した $\forall$ と $\exists$ が混在する箇所である。

---
## 2026-09-14

**Dinur et al. 2003 _ Revealing information while preserving privacy**

ここで示されている定理は、漸近的な評価に依存しており、そのまま実システムに組み込めるパラメータ設計にはなっていない。

理論上は $o(\sqrt{n})$ や $\tilde{O}(\sqrt{\mathcal{T}(n)})$ といったオーダーによってプライバシーとノイズの境界が示されているが、現実のデータベースは数万から数千万といった有限のサイズ $n$ を持つ。実システムを構築する際には、数式上のオーダーに隠された定数倍の部分がノイズの絶対量に大きく影響するため、オーダーの議論だけでは具体的なノイズの付加量を決定できない。

攻撃者の能力を多項式時間や指数関数時間として分類するアプローチは、理論的な不可能性を示す上では強力な枠組みだが、現実のシステム運用において想定すべき攻撃者の具体的な計算リソースを定量化して防衛ラインを引くには、抽象的すぎる。

一方で、本論文が示した「ノイズの大きさが $\Omega(\sqrt{n})$ を下回る場合、どのような摂動手法を用いても多項式時間の攻撃者によってプライバシーが破られる」という不可能性定理（Theorem 3）は、それまで各所で提案されていたヒューリスティックなデータ加工やクエリ制限が、本質的に脆弱であることを数学的に証明したという点で意義深い。

この極限的で抽象的な議論を基礎として、有限の $n$ に対し、データ所有者が許容できるリスクを具体的な数値パラメータとして制御する差分プライバシーなどの実践的な枠組みへと発展していく。

というわけで、次は、Dwork et al. 2006 _ Calibrating Noise to Sensitivity in Private Data Analysis を読む。シルバーウィークで読み切れるかな？

---
## 2026-09-13

**Dinur et al. 2003 _ Revealing information while preserving privacy**

クエリ回数が定数に抑えられる環境であれば、定理5により、実用的なノイズ量でプライバシーを保護できる可能性がある。

実運用において、クエリ回数を定数に制限のアプローチはしばしば登場する。

- 集計レポートの静的公開: 国勢調査のように、固定された集計結果を一度だけ公開する形式
    
- 監査とアクセス制限: システム側で1ユーザーあたりのクエリ発行回数に厳格な上限を設ける
    
- 定型データ抽出: 分析者が対話的に任意のクエリを発行するのではなく、あらかじめ設定された限られたパターンの統計値のみを返す

実際のシステムにおいてクエリの発行回数や自由度に制限をかける設計は、データの有用性を保ちつつプライバシーの漏洩を防ぐための理にかなった手法となるはず。

---
## 2026-09-12

ナビエ＝ストークスのニュースを聞いて、初めてLeanというものを知った。

証明をLeanで書く（あるいは少なくとも読める）ことは、避けて通れないリテラシーになるのだろう。あと、単純に、楽しそうではある。

数学を自習してると、自分の証明が合っているのか誰も採点してくれないという不安が常につきまとっていたが、コンピューターが判定してくれるのならありがたい。

証明を機械がやってくれるようになっても、それを人間が理解する取り組みは別だから、Knowledge Base を書きてめていくことが無駄になるわけではない。

---
## 2026-09-05

平日は仕事に全振りしないと、今月は無理そう。勉強は休日だけ。

Dinur et al. 2003 _ *Revealing information while preserving privacy*

の精読を進めているが、半日使って1章までしか読めなかった。

現在、データを加工して公開する匿名化は、特に複雑なデータセットにおいては実質的に破綻したという結論が主流になっている。それが決定的になったのがネットフリックスの2008年の論文。この論文は2003年が、匿名化を考えないものとして書いてる。考えないというか、「それでは全く通用しない」と早々に切り捨てて、別の前提に移行している。

データ公開からクエリ応答モデルへこのパラダイムシフトはこの頃からすでに始まっていたのか？少なくとも学術の界隈では。法令は何年遅れてんだ？

ちょっとまとめてみよう。

**学術界隈**

- 単純な匿名化の限界とk-匿名性の誕生（初期の金字塔）
	- "k-anonymity: A model for protecting privacy" (Latanya Sweeney, 2002)
		- 氏名などの直接的識別子を消すだけの匿名化がなぜ破られるのかを実証し、準識別子（Quasi-Identifiers）とk-匿名性という概念を数学的に定義。
	- "Simple Demographics Often Identify People Uniquely" (Latanya Sweeney, 2000)
		- 郵便番号、性別、生年月日の3つだけでアメリカ人口の87%が一意に特定可能であるという事実をデータ検証によって突きつけた。
- k-匿名性の数学的脆弱性とその拡張
	- "l-diversity: Privacy beyond k-anonymity" (Machanavajjhala et al., 2007)
		- k-匿名性を満たしていても、同質性攻撃（グループ全員が同じ属性を持つ場合）や背景知識攻撃によってプライバシーが100%破綻することを数学的に証明し、属性の多様性を求める$l$-多様性を提案。
	- "t-closeness: Privacy beyond k-anonymity and l-diversity" (Li et al., 2007)
		- $l$-多様性でも防げない確率分布の偏りによる情報漏洩を指摘し、元のデータ全体の分布と匿名化されたグループの分布の距離を計算して制限する$t$-近似を定義。
- 高次元データにおける匿名化手法の事実上の終焉
	- "Robust De-anonymization of Large Sparse Datasets" (Narayanan & Shmatikov, 2008)
		- 匿名化されて公開されたNetflixの映画レビューデータ（誰がどの映画を何点と評価したか）と、IMDbの公開データを照合することで、個人を特定できることを実証。購買履歴や行動履歴のような属性が多い高次元なデータにおいて、従来のデータを加工する匿名化アプローチが実質的に無意味であることを決定づけた。

**法制度**
- 2003年（日本の個人情報保護法 成立）: 「氏名などの識別子を消せば安全」という、学術界がちょうど切り捨てたばかりの古いパラダイムに立脚していた。
- 2015年（日本の個人情報保護法 改正）: 学術界（Netflixの事例含む）が、匿名化したデータセットの公開は外部知識との結合で破綻すると結論付けたずっと後に、なぜか匿名加工情報という使い物にならない概念を新設し、データセットの公開（第三者提供）を法的に後押しする方向へ動いた。

法律がこれほど遅れるのは、法制度が「氏名をマスキングしたか」という人間の直感的に訴えるルール作りを好む（紙の書類の黒塗りの発想から進化してない）のに対し、学術界の最前線が数学的な情報漏洩量の計算という非直感的な領域に突入してしまったからだろう。

米国の国勢調査局や巨大テック企業はすでに法律を飛び越えて差分プライバシー（クエリ応答モデル）を実装しているが、法律がこの数学的現実を正確に捉えられるようになるには、相当な時間がかかりそうだ。

---
### Archives

#### 2026
- [2026-08](archives/2026/2026-08.md)


